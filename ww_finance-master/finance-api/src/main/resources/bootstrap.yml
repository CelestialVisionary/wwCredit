spring:
  profiles:
    active: dev # 环境设置
  application:
    name: service-core # 服务名
  config:
    import-check:
      enabled: false

  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false

logging:
  level:
    com:
      alibaba:
        nacos:
          client: warn
