<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wwfinance.mapper.UserAccountMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.wwfinance.entity.UserAccount">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="amount" property="amount" />
        <result column="freeze_amount" property="freezeAmount" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="is_deleted" property="deleted" />
        <result column="version" property="version" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, amount, freeze_amount, create_time, update_time, is_deleted, version
    </sql>

    <update id="updateAccount">
        update user_account
        set
            amount = amount + #{amount},
            freeze_amount = freeze_amount + #{freezeAmount}
        where user_id = (select id from user where bind_code = #{bindCode})
    </update>

</mapper>
